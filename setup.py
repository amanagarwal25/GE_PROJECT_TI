from setuptools import setup, find_packages

setup(
  description='Great Expectations demo project',
  version='0.0.1',
  install_requires=['pandas'],
  packages=find_packages(),
  name='ge_example_project',
  license='Apache-2.0',
)
